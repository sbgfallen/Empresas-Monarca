{
  "name": "empresas-monarca",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm run build",
    "start": "cd server && node server.js",
    "db:init": "node -e \"require('dotenv').config({path:'server/.env'}); require('./server/init').initializeDatabase().then(()=>console.log('Done')).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "@vercel/node": "^7.0.0",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^1.41.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "multer-storage-cloudinary": "^4.0.0",
    "nodemailer": "^9.0.5",
    "pg": "^8.23.0"
  }
}
